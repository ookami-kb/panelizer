name: example
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  panelizer:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  mews_pedantic: ^0.26.0

flutter:
  uses-material-design: true
